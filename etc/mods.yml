# Mods.

root:
  - units:
      - type: noise
      - type: text
        params:
          text: "SOIR"
          position: center
        bindings:
          digitakt.kick: ["shiftLetters"]
  - units:
      - type: debug
