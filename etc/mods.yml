# Mods.

root:
  - units:
      - type: noise
        bindings:
          digitakt.kick: ["switchMode"]
      - type: text
        params:
          text: "SOIR"
          position: center
        bindings:
          digitakt.kick: ["shiftLetters"]
      - type: exp
  - units:
      - type: debug
