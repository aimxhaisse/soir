# Mods.

root:
  - units:
      - type: shader
      - type: noise
        bindings:
          digitakt.kick: ["switchMode"]
      - type: text
        params:
          text: "SOIR"
          position: center
        bindings:
          digitakt.kick: ["shiftLetters"]
